name: flutter_application_1
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # =========================
  # CORE & UI
  # =========================
  cupertino_icons: ^1.0.6
  font_awesome_flutter: ^10.12.0
  custom_line_indicator_bottom_navbar: ^0.0.8
  intl: ^0.20.2

  # =========================
  # AUTH & FIREBASE
  # =========================
  firebase_core: ^2.27.0
  firebase_auth: ^4.17.0
  google_sign_in: ^6.2.2

  # =========================
  # STORAGE & STATE
  # =========================
  shared_preferences: ^2.5.4
  get: ^4.7.3

  # =========================
  # NETWORKING
  # =========================
  http: ^1.6.0
  http_parser: ^4.1.2
  json_annotation: ^4.9.0

  # =========================
  # IMAGE & CAMERA
  # =========================
  image_picker: ^1.0.7
  camera: ^0.11.3

  # =========================
  # POSE DETECTION (ML KIT) - VERSI YANG KOMPATIBEL
  # =========================
  google_mlkit_pose_detection: ^0.12.0
  google_mlkit_commons: ^0.8.1  # UPGRADE KE VERSI INI

  # =========================
  # TENSORFLOW LITE (AI MODEL)
  # =========================
  tflite_flutter: ^0.9.0  # Versi stabil

  # =========================
  # MATH & GEOMETRY
  # =========================
  vector_math: ^2.1.4
  path_provider: ^2.1.0

  # =========================
  # TEXT TO SPEECH
  # =========================
  flutter_tts: ^3.8.3

  # =========================
  # PERMISSIONS
  # =========================
  permission_handler: ^11.0.1  # Downgrade ke versi stabil

  # =========================
  # VIBRATION
  # =========================
  vibration: ^1.7.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/images/google_logo.png
    - assets/models/push-up/pushup_model.tflite